// Practice programming daily.

/* This demonstrates the use of the switch statement.
If it were Python, it would be a match statetment.*/

// This is what the code for function calls for unit creation 
// may look in some software. 

switch(menu_event) {
    case BUILD:
        build_structure(Event_data);
        break;
    case RECRUIT:
        recruit_unit(event_data);
        break;
    case MOVE:
        move_unit(event_data);
        break;
    case DESTROY: //additional for this example
        destroy_unit(event_data);
        break;
    
}
